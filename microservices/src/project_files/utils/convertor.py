def celsius_to_fahrenheit(value: float) -> float:
    """
    for tem celsius_to_fahrenheit conversion method
    """
    return (value * 1.8) + 32


def fahrenheit_to_celsius(value: float) -> float:
    """
    for tem fahrenheit_to_celsius conversion method
    """
    return (value - 32) / 1.8
